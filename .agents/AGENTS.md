# Rules

- **Commit Messages**: Always keep commit messages extremely short (2-4 words total). Do NOT use full sentences. Every commit message must start with a short prefix (e.g., `fix`, `clean`) WITHOUT any colons, commas, or special punctuation. Follow the prefix with 1-3 words describing the subject (e.g. `clean repo`, `clean readme`, `fix cellhawk py`).
